# SPDX-FileCopyrightText: (C) 2025 Intel Corporation
# SPDX-License-Identifier: Apache-2.0


# default value is set to 1
# The default value is set to 1. If it is set to 0, the user has control over 
# the POOL_NAME and BRIDGE_NAME (Libvirt network interface).
STANDALONE=1

CLUSTER="kind.internal"
# IO Flow Configurations
ONBOARDING_USERNAME="ONBOARDING_USER"
ONBOARDING_PASSWORD="ONBOARDING_PASSWORD"

# NIO Flow Configurations
PROJECT_NAME="infra-proj-1"
PROJECT_API_USER="edgeinfra-infra-api-user"
PROJECT_API_PASSWORD=""

# VM Resources
RAM_SIZE=8192
NO_OF_CPUS=4
SDA_DISK_SIZE="64G"
SDB_DISK_SIZE="64G"
LIBVIRT_DRIVER="kvm"

# Provisioned VM User credentials
USERNAME="user"
PASSWORD="user"
CI_CONFIG="false"

# Optional: Advance Settings
# Below configurations are intended only for CI/CD
# Specify the name of the NIC interface
INTF_NAME=""
# Specify the name of the provisioning VM
VM_NAME=""
# This setting is applicable only when STANDALONE is set to 0. 
# If STANDALONE is set to 1, POOL_NAME defaults to 'default'.
POOL_NAME="default"
# The name of the libvirt network interface
BRIDGE_NAME=""
